import React from 'react';
import ReactDOM from 'react-dom/client';
import LazyMain from './AsyncComponents/LazyMain';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
 <div>
  <LazyMain/>
 </div>
);
