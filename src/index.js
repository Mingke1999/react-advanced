import React from 'react';
import ReactDOM from 'react-dom/client';
import ErrorMain from './ErrorBorder/ErrorMain';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
 <div>
  <ErrorMain/>
 </div>
);
